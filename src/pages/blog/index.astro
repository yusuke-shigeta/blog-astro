---
import BaseLayout from "../../layouts/BaseLayout.astro";
import { getCollection } from "astro:content";

const blogCollections = await getCollection("blog");

const pageTitle = "Blog";
---

<BaseLayout pageTitle={pageTitle}>
  <ul>
    {
      blogCollections.map((blogCollection) => (
        <li>
          <a href={`/blog/${blogCollection.slug}`}>
            {/* <img src={blogCollection.data.thumbnail} alt={blogCollection.data.title} /> */}
            <p>{blogCollection.data.title}</p>
          </a>
        </li>
      ))
    }
  </ul>
</BaseLayout>
