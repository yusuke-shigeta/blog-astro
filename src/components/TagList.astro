---
const { tags } = Astro.props;
---

<ul class="tags">
  {
    tags.map((tag) => (
      <li class="tag">
        <a class="tag-link" href={`/blog/tag/${tag}`}>
          {tag}
        </a>
      </li>
    ))
  }
</ul>

<style>
  .tags {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
  }
</style>
